# MosaicVPN v0.3.56

Релиз качества: закрыты три дефекта, найденные при сплошном аудите кодовой базы,
устранена корневая причина крашей на телефонах и приведена в порядок раздача
настольных сборок.

## Главное: устранена причина крашей на реальных телефонах

Версии **v0.3.53 – v0.3.55** раздавались с сайта в виде локальной сборки. В
репозитории лежит отладочная заглушка `libbox.aar`, собранная только под
эмулятор (`x86_64`), поэтому в тех APK ядро sing-box для ARM **физически
отсутствовало**: на обычном телефоне приложение падало сразу при попытке
подключения. Симптом при этом выглядел как ошибка конфигурации, что уводило
диагностику в сторону.

Проверяемый признак: настоящая мультиархитектурная сборка весит **~65 МБ**,
а сборка только под эмулятор — 53–56 МБ. Сайт отдавал 56 МБ.

Что сделано:
- APK v0.3.56 берётся строго из артефактов CI и содержит
  `arm64-v8a` + `armeabi-v7a` (проверено `aapt2 dump badging`: `native-code:
  'arm64-v8a' 'armeabi-v7a'`).
- Добавлен `scripts/publish_apk_to_landing.sh`: публикация возможна только из
  релиза GitHub, с жёсткими проверками на наличие ARM-библиотек, размер >60 МБ
  и совпадение md5 с тем, что реально отдаёт сайт.
- Ссылка на скачивание переведена на версионированное имя файла, чтобы
  4-часовой кеш Cloudflare больше не отдавал предыдущую сборку после релиза.

## Исправлено

### Кабинет больше не выглядит пустым при холодном старте
`subscriptionsProvider` был `FutureProvider.autoDispose` без межсессионного кэша:
на каждом запуске список подписок и маршрутов был пуст до ответа демона, что
читалось пользователями как «подписка пропала». Добавлена схема
stale-while-revalidate — снимок прошлой сессии сохраняется в `SharedPreferences`
и отрисовывается мгновенно на первом кадре, затем молча заменяется свежими
данными. При недоступном демоне (offline, служба ещё не поднялась) показывается
кэш вместо экрана ошибки. Повреждённый снимок самоочищается и не блокирует старт.
Подключено на Dashboard и в инвентаре маршрутов.

### В меню станции не было пункта «Отключить»
Контекстное меню сервера на Dashboard содержало только ping / копирование /
редактирование конфига. Пользователь, открывший меню ради отключения, не находил
его вовсе. Добавлен пункт **Disconnect** (виден только у активной станции)
и соответствующий обработчик с инвалидацией статуса.

### Раздача desktop-сборок отставала на две версии
Сайт и Telegram-бот ссылались на артефакты **v0.3.54** для Windows (EXE/ZIP) и
Linux (DEB/TAR.GZ), хотя CI собрал полный набор и для v0.3.55 — обновлялся только
APK. Все пять ссылок переведены на v0.3.56, включая `MosaicVPN_0.3.56_amd64.deb`
(имя без префикса `v`, из-за чего ссылка ранее не попадала под общую замену).

## Тесты и качество

- `flutter analyze lib/` — **0 issues**.
- Flutter: **102/102** теста зелёные.
- Go: **13/13** пакетов зелёные.
- Обновлён устаревший golden `mobile_advanced_tools.png`: эталон был снят в
  v0.3.43, а в v0.3.53 в список инструментов добавился пункт Egresses — тест
  падал с diff 17.23% на чистом HEAD. Дополнительно добавлен семантический
  guard, который проверяет состав меню по подписям, чтобы `--update-goldens`
  не мог молча «благословить» удаление экрана.
- Новый набор из 6 тестов на кэш подписок: round-trip, пустой кэш, повреждённый
  JSON, JSON-объект вместо списка, очистка, санитизация URL с завершающей точкой.
